# Voice Medical Agent - Dependencies
# ═══════════════════════════════

# ── Core (Required) ─────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
openai>=1.12.0
python-dotenv>=1.0.0
scikit-learn>=1.4.0    # Medical knowledge base TF-IDF search
edge-tts>=6.1.0         # FREE TTS (Microsoft Edge voices)
faster-whisper>=1.0.0   # FREE STT (local Whisper model)

# ── Optional: Google Cloud STT/TTS ──────────
# google-cloud-speech>=2.25.0
# google-cloud-texttospeech>=2.16.0

# ── Optional: Azure Speech (Recommended for Sindhi) ──
# azure-cognitiveservices-speech>=1.37.0

# ── Optional: ElevenLabs TTS (Premium) ─────
# httpx>=0.27.0

# ── Optional: Alibaba Cloud DashScope (Qwen Models) ──
# dashscope>=1.20.0
